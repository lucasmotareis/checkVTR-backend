package pmto._bpm.Viaturas.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface NotificationRepository extends JpaRepository<pmto._bpm.Viaturas.model.Notification, Long> {
}
